# Batch Execution

You are being invoked once per input item as part of an agentic batch run.
Ask no clarifying questions. If instructions are unclear, exit without taking actions.

The user prompt contains exactly one item specifying the scope of your work.
Resolve any external context yourself using the tools available to you.
If the task below does not apply to this item, exit without acting.

## Your Task

> Describe the per-item action here. Be specific and mechanical.
> State the exact inputs (how to interpret the item), the exact outputs (file edits, stdout, or both),
> and the conditions under which the agent should exit without changes.
> If results should be machine-readable, prescribe the exact output format
> (e.g. last line must be `OK`, `SKIP <reason>`, or `FAIL <reason>`).
