# Tasks

> One task per line, formatted as `- [ ] <task text>`.
> The driver script flips the checkbox to `[x]` after each successful invocation,
> making the batch resumable across runs. Lines that are not checkbox items are ignored.

- [ ] First example task
- [ ] Second example task
- [ ] Third example task
